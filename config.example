# Copy this file to "config" and fill in local values.
#   cp config.example config
#
# Do not put real URLs, API keys, dataset IDs, customer names, or
# environment-specific examples in this template.

DIFY_BASE_URL=
DIFY_APP_KEY=
DIFY_DATASET_API_KEY=
DIFY_DATASET_IDS=

# Client installs can use a hosted gateway instead of local Dify API keys.
# Leave empty on the Dify host itself.
DIFY_RAG_GATEWAY_URL=
DIFY_RAG_REMOTE_GATEWAY_URL=
DIFY_RAG_SHARED_SECRET=
DIFY_RAG_CLOUDFLARE_ACCESS=auto
DIFY_RAG_CLOUDFLARED_BIN=cloudflared

# Remote MCP is the public Custom Connector endpoint.
# Put Cloudflare Tunnel in front of http://127.0.0.1:8788/rag.
DIFY_RAG_REMOTE_MCP_HOST=127.0.0.1
DIFY_RAG_REMOTE_MCP_PORT=8788
DIFY_RAG_REMOTE_MCP_PATH=/rag

# Optional path prefix for published gateway URLs such as
# https://api.example.com/rag/search.
DIFY_RAG_GATEWAY_PATH_PREFIX=

# Comma-separated email allowlist for add_knowledge.
# Search remains available to everyone who can access the connector.
DIFY_RAG_ADD_ALLOWED_EMAILS=
